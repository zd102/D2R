# D2R project workflow

用户已于 2026-09-10 授权：本项目以后完成修改并验证通过后，自动使用 Git 提交并推送。除非用户在当前任务另有要求，不要在验证通过后停下来重复询问是否提交或推送。只读查询不需要产生提交。

- 当前项目远程为 `https://github.com/zd102/D2R.git`，默认分支为 `main`。操作前确认实际分支、上游和工作区状态，保留用户已有改动。
- 游戏逻辑或 TypeScript 修改运行 `npm test` 与 `npm run build`，并运行相关浏览器回归；地图和场景修改运行 `npm run test:maps`、`npm run test:scenery`。浏览器测试默认使用已启动的 `http://127.0.0.1:5173`，可按测试脚本支持情况指定 `BASE_URL`。
- 文档或工作约定修改检查内容、链接与 `git diff --check`；无需仅因文档变化重跑游戏构建。
- 验证前记录已有产物，验证后只清理本次生成的文件。不得整体清理已有 `.verification/`、存档或依赖目录。故障必须修复或明确报告，不能带着未解释的失败宣称通过。
- 审查最终差异，只暂存本任务文件，提交后推送当前已授权分支。使用本机凭据管理器，不把密码或令牌写入代码、文档、技能、命令参数或远程地址。
- 推送成功后核对远程分支 SHA 与本地 `HEAD`，报告提交和验证结果。网络错误可有限重试；不得强制推送或覆盖他人历史。失败时保留本地提交并明确说明未推送。
- 本机可使用 `d2r-verify-commit-push` 技能复用已验证流程；技能缺失时按本文件执行。此授权仅适用于本项目，不扩展到其他仓库。
